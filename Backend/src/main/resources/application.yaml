spring:
  application:
    name: cinepass
  profiles:
    active: dev # default profile

  datasource:
    url: jdbc:postgresql://localhost:5432/cinepass
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 2700000